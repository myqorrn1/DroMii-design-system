# Claude Design 프롬프트

이 묶음을 첨부하고 아래를 순서대로 붙여넣습니다.
**한 번에 다 요청하지 마세요** — 하나씩 확정하고 다음으로 가야 기준이 흔들리지 않습니다.

---

## 0. 시작 — 재료 읽히기

```
첨부한 드로미 디자인시스템 묶음으로 작업할 거야.

먼저 context/ 세 파일을 읽어줘. 순서대로:
- 01-as-is.md   우리 제품 두 개(K-AQUAS, D-ROAD)의 코드 실측 데이터
- 02-keep.md    그대로 쓸 것과 고칠 것, 그 판단 기준
- 03-rules.md   시스템 규칙 12개

그다음 tokens.css와 foundations/, components/를 봐줘.
이건 위 재료로 이미 만든 첫 결과물이야.

읽고 나서 다음 세 가지를 정리해줘:
1. 이 시스템이 이미 정해둔 것 (건드리면 안 되는 것)
2. 아직 비어 있는 것
3. 네가 보기에 다시 봐야 할 것 — 근거와 함께

아직 아무것도 만들지 마. 파악한 것만 알려줘.
```

---

## 1. 컴포넌트 이어 만들기

`0`에서 정리가 끝난 뒤, 하나씩 요청합니다.

```
입력 요소를 components/input.html 로 만들어줘.

형식은 components/button.html 과 똑같이:
- 첫 줄에 <!-- @dsCard group="Components" name="…" subtitle="…" --> 마커
- ../tokens.css 링크
- 색은 전부 역할 토큰으로. 하드코딩 hex 금지
- data-product 로 K-AQUAS / D-ROAD 두 벌을 나란히 보여주기

담을 것:
text input · select · textarea · checkbox · radio · switch
각각 default / focus / error / disabled 네 상태.

context/03-rules.md 의 규칙을 지켜줘. 특히:
- 다크의 입력 배경은 카드보다 어둡게 (#1E2027), 라이트는 배경보다 밝게 (흰색)
- 라벨은 항상 입력란 위. 플레이스홀더를 라벨로 대신하지 않음
- helper 텍스트 자리는 비어 있어도 높이 유지 (에러가 뜰 때 아래 필드가 밀리지 않도록)
- checkbox·radio 18px, switch 36×20, knob 16px

실제 문구는 context/01-as-is.md 의 도메인 어휘를 써줘.
```

같은 틀로 이어서 요청할 것:

| 순서 | 파일 | 담을 것 |
|---|---|---|
| 2 | `components/badge.html` | 상태 배지 5종 · 칩 · 카운트 배지 · 상태 점 |
| 3 | `components/toast.html` | 토스트 4종 · 쌓임 · 액션 포함 변형 |
| 4 | `components/banner.html` | 인라인 배너 4종 · 항목 목록형 |
| 5 | `components/dialog.html` | 확인 다이얼로그 — 파괴적 / 일반 |
| 6 | `components/modal.html` | 모달 셸 · 폭 4단계 (sm 400 / md 448 / lg 640 / xl 1000) |
| 7 | `components/table.html` | 데이터 테이블 · 정렬 · 선택 · 페이지네이션 · 빈 상태 2종 · 스켈레톤 |
| 8 | `components/form.html` | 폼 레이아웃 · 섹션 · 필수 표시 · 에러 요약 · 액션바 |
| 9 | `foundations/spacing.html` | 4px 그리드 11단계 · radius 6단계 |
| 10 | `foundations/elevation.html` | 그림자 3단계 · z-index 8단계 |
| 11 | `foundations/icons.html` | UI 아이콘 세트 + 그리기 규칙 |

---

## 2. 아이콘 — 유일하게 새로 그려야 하는 것

```
foundations/icons.html 을 만들어줘. 이건 코드에 참고할 게 없어서 새로 그려야 해.

현재 상태: PNG 152개(K-AQUAS 114 + D-ROAD 38)와 MUI 아이콘 22종이 섞여 있고,
같은 뜻의 아이콘(닫기·삭제·편집·다운로드)을 두 제품이 각자 다른 그림으로 갖고 있어.
지도 심볼로 쓰이는 PNG는 확대하면 흐려져.

두 층으로 나눠줘:

1) UI 아이콘 — 세 제품 공통, 인라인 SVG
   닫기 · 검색 · 추가 · 편집 · 삭제 · 다운로드 · 업로드 · 정보 ·
   더보기 · 설정 · 사용자 · 필터 · 새로고침 · 캘린더 · 외부링크 · 복사
   24px 격자 · stroke 1.5px 고정 · 끝단 round · currentColor 상속
   16 / 20 / 24px 세 크기 비교도 함께

2) 그리기 규칙 — 도메인 아이콘까지 관통할 기준
   격자 구조, 선 굵기, 모서리 반경, 광학 정렬, 하지 말 것 예시

도메인 아이콘(K-AQUAS 댐·수질 / D-ROAD 포트홀·균열)은 이번엔 그리지 말고,
"같은 격자·같은 선 굵기·같은 모서리 처리만 지키면 된다"는 규칙만 정리해줘.
댐과 포트홀을 같게 만들 필요는 없어. 나란히 놓았을 때 같은 손으로 그린 것처럼
보이면 돼.

색은 넣지 마. 전부 currentColor 로 그려서 토큰이 색을 주입하게.
```

---

## 3. 개선 요청 — 이미 만든 것을 다시 볼 때

```
[파일명] 을 다시 봐줘.

context/02-keep.md 의 판단 기준(빈도 · 일관성 · 맥락 · 의도)으로 평가해줘:
- 코드에 이미 있던 걸 존중했는가
- 라이브러리 기본값이나 회피책이 섞여 들어가지 않았는가
- 두 제품에서 구조가 정말 같은가

바꿀 게 있으면 근거와 함께 알려주고, 내가 확인한 뒤에 고쳐줘.
값을 임의로 바꾸지는 마.
```

---

## 4. 새 제품(D-FIND) 추가할 때

```
D-FIND 액센트 램프를 만들어줘. 기준색은 [#XXXXXX] 이고 [라이트/다크] 테마야.

tokens.css 의 [data-product="d-road"] 블록을 참고해서 같은 구조로:
- 액센트 램프 10단계 (50~900)
- 역할 토큰 전체 (primary · primary-solid · hover · pressed · text · focus-ring ·
  surface · border · text · row-hover · row-selected)

주의:
- 기준색이 램프의 몇 번 자리에 오는지는 명도에 따라 달라져.
  K-AQUAS는 500, D-ROAD는 400이야. 다크 테마면 배경보다 밝아야 하니 보통 400 근처.
- 채움 버튼에 글자를 얹었을 때 대비가 4.5:1 미만이면 알려줘.
  한 단계 아래를 solid 로 쓸지, 어두운 글자를 쓸지 판단해야 해.
- 나머지 토큰(간격·라운드·타이포·깊이·z-index)은 공통이라 손대지 마.
```

---

## 지켜야 할 것 — 매번 상기시킬 규칙

프롬프트가 길어지면 이 네 줄만 덧붙여도 됩니다.

```
- 색은 역할 토큰으로만. 램프 번호(accent-500)를 직접 참조하지 말 것
- 간격은 4px 그리드. 5·15·25px 금지
- 새 색 조합을 만들면 대비를 계산해서 표기할 것 (본문 4.5:1, 큰 글자·아이콘 3:1)
- 한국어 UI 문구로. lorem 금지, context/01-as-is.md 의 실제 도메인 어휘 사용
```
