# DroMii Core 구축 로드맵

제품 코드에서 출발해 공용화 가치를 판단하고 KRDS·접근성을 점검한다. 단계가 끝날 때마다
`npm run check` → `git diff --check` → 브라우저 확인 → 커밋·`main` 푸시 → Pages 확인을
수행하고 [`작업 인계`](HANDOFF.md)와 [`현재 상태`](STATUS.md)를 갱신한다.

## 단계 0 — 기존 미커밋 작업 마무리 · 완료

- [x] 선택형 로그 표의 가상 검색·선택·삭제 확인 견본 검증
- [x] 320px·512px 유효 화면에서 앱 셸·표 대응 검증
- [x] 커밋 `84b8b26`을 `main`에 푸시하고 GitHub Pages 반영 확인

실제 브라우저 확대율 200% 시험은 아직 완료하지 않았다. 유효 화면 폭 검사와 구분해
[`현재 상태`](STATUS.md)에 남긴다.

## 단계 1 — 방향 문서 재정비 · 완료

- [x] 제품 추출 → Core 공통화 → KRDS·접근성 점검 순서로 README·인계 문서 수정
- [x] [`KRDS 채택 범위와 예외`](PUBLIC_SECTOR_DIRECTION.md)에 따를 것·방법만 빌릴 것·
  따르지 않을 것과 예외 목록 집중
- [x] 공식성·준수 상태의 반복 설명을 기준 문서 링크로 정리
- [x] [`적용 전략`](APPLICATION.md)에 모습 보존 토큰 연결, MUI 테마, 점진 전환 순서 기록

## 단계 2 — 제품 소스 재감사 · 완료

- [x] 운영 D-ROAD 서버 `/home/dromii/dromii_v5_react/src` 전체를 색·글자 크기와 단위
  (`vw` 포함)·간격·radius·컨트롤 높이·구조·MUI 사용·접근성으로 감사
- [x] 로컬 `K-AQUAS_Code/src`의 같은 항목과 8/28 스냅샷 이후 변경분 확인
- [x] [`제품 소스 재감사`](../context/06-product-audit-2026-09-23.md) 작성,
  `context/01-as-is.md`의 낡은 근거 표시

제품 코드와 서버는 **읽기만** 한다. 이전 Vite 후보 소스를 운영 D-ROAD의 근거로 쓰지 않는다.

## 단계 3 — 파운데이션 결정 요청 · 완료

- [x] [제품 소스 재감사](../context/06-product-audit-2026-09-23.md)의 실측값·현행 Core·
  KRDS에서 큰 차이만 추려 결정 요청을 준비했다. 별도 단계 3 문서는 만들지 않는다.
- [x] 본문 글자 크기와 `default / compact`, D-ROAD 다크 기본값과 고대비 자리,
  조밀한 컨트롤 높이마다 현재 값·KRDS 기준·선택지 2~3개·장단점·화면 영향·추천안을
  **대화에서 보고하고 소유자 결정을 받았다**.

승인된 KRDS 차이는 [`예외 목록`](PUBLIC_SECTOR_DIRECTION.md)에 기록했다.
제품별 기존 요소와 Core의 세부 매핑은 [제품 적용 전략](APPLICATION.md)에 따라
실제 적용을 계획할 때 만든다.

## 단계 4 — 파운데이션·토큰 재구성 · 구현 후 검토

- [x] 승인된 값으로 `tokens/source.json`을 reference / semantic / component 3계층으로 재구성
- [x] `brand / scheme / density` 축 분리, `scheme`에 고대비 자리 마련
- [x] 명도 40/50/70/90 등급의 역할 색상 대비 규칙과 `scripts/check.mjs` 검사 추가
- [x] 글자 크기 상대 단위 전환, CSS·HTML 견본·문서 갱신
- [x] K-AQUAS·D-ROAD [전후 비교 견본](../foundations/comparison.html) 작성
- [ ] 브라우저 시각 검증과 검사 결과 보고 뒤 소유자 승인

**여기서 다시 소유자 승인을 받는다.**

## 단계 5 — 업무 패턴 · 단계 4 승인 후

1. 표·검색의 서버 실패·부분 성공·권한 제한과 긴 표
2. 위험 동작 확인과 오류 복구
3. 업로드 대기·진행·실패·재시도·부분 성공
4. 지도 레이어·범례·도구·객체 선택·오류

각 패턴은 관련 KRDS 기본 패턴을 점검 목록으로 쓰고 정상·빈 상태·오류·권한 제한·로딩을
모두 설계한다. 대표 화면, React 제품 적용, 실제 운영 배포는 이후 범위다.
